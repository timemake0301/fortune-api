{
  "name": "fortune-api",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "node scripts/migrate.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/node": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "3.2.x"
  }
}
